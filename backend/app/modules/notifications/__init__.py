"""Notification pipeline (SPEC §6.8).

Phase 2 ships the write path (`service.py`) and rendering (`rendering.py`) —
enough for the auth flow's invitation and OTP mail. Recipient resolution, the
preference matrix, quiet hours, digests, and the in-app feed land in Phase 7.
"""
